server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kotlin-test?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: sua7308!?
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
